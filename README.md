# Run

Run is a very simple program. You execute the "run.sh" script and give is a code file in one of the supported programming languages. It will then create and open a docker container to run the code. Essentially, this is a docker-based programming language agnostic compiler. 

Command syntax:
./run [code file path]

Supported file types:
- .py
- .sh
- .c
- .cpp